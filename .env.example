# JWT Secret Key for token encoding/decoding
# Generate a secure random key for development (e.g., using `rails secret`)
JWT_SECRET_KEY=your_jwt_secret_key_here

# Database Configuration
# DATABASE_URL=postgres://username:password@localhost:5432/database_name

# Redis Configuration (if needed)
# REDIS_URL=redis://localhost:6379/0

# Mailer Configuration (if needed)
# SMTP_USERNAME=your_smtp_username
# SMTP_PASSWORD=your_smtp_password
# SMTP_DOMAIN=your_domain.com
# SMTP_ADDRESS=smtp.your_provider.com
# SMTP_PORT=587 